export async function setPreviousData(previousStage, setPreviousStage) {
  try {
    console.log('entering setPreviousData');
    // setPreviousStage(previousStage);
  } catch (error) {
    console.log(error);
  }
}
